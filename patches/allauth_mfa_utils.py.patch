Index: b/allauth/mfa/utils.py
===================================================================
--- a/allauth/mfa/utils.py
+++ b/allauth/mfa/utils.py
@@ -9,5 +9,5 @@
     return get_adapter().decrypt(encrypted_text)
 
 
-def is_mfa_enabled(user, types=None):
-    return get_adapter().is_mfa_enabled(user, types=types)
+def is_mfa_enabled(user=None, request=None):
+    return get_adapter().is_mfa_enabled(user, request)
